import json

def start_analysis():
    print("🔍 جاري تحليل البيانات التقنية للمؤسسات...")

    try:
        # فتح ملف البيانات الذي أنشأه سكريبت test_json
        with open("dz.json", "r") as f:
            data = json.load(f)

        # استخراج المعلومات بذكاء
        inst = data.get("institution", "غير معروف")
        status = data.get("status", "غير معروف")

        print(f"\n📊 التقرير الحالي:")
        print(f"المؤسسة: {inst}")
        print(f"الحالة التقنية: {status}")

        # منطق اتخاذ القرار (AI Logic بسيط)
        if status == "Active":
            print("💡 نصيحة: النظام مستقر، يمكنك إجراء عمليات الدفع.")
        else:
            print("⚠️ تحذير سيبراني: يرجى التحقق من الشبكة قبل المحاولة.")

    except FileNotFoundError:
        print("❌ خطأ: ملف البيانات dz.json غير موجود. قم بتشغيل test_json.py أولاً.")

if __name__ == "__main__":
    start_analysis()
